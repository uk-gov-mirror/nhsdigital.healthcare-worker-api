name: "Lint OpenAPI specification"
description: "Lint the OpenAPI specification using Redocly CLI"
runs:
  using: "composite"
  steps:
    - name: "Lint OpenAPI specification"
      shell: bash
      run: |
        make lint-specification
